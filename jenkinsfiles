pipeline {
    agent any
    stages {
        stage('Clone sources') {
            steps {
                git url: 'https://github.com/imkunalll/HrManagersb.git'
            }
        }
        stage('SonarQube analysis') {
            steps {
                withSonarQubeEnv('SonarQube') {
                   bat "mvn sonar:sonar -Dsonar.projectKey=HrManagersb -Dsonar.host.url=http://localhost:9000 -Dsonar.login=94dde6e37848ed874473de3c77328df87fecae31"
                }
            }
        }
        stage("Quality gate") {
            steps {
                waitForQualityGate abortPipeline: true
            }
        }

        stage('Requirement') {
            steps {
                echo 'Requirement gathering'
            }
        }
        
        stage('Planning') {
            steps {
                echo 'Planning'
            }
        }
        
        stage('Designing') {
            steps {
                echo 'Designing'
            }
        }
        
        stage('Developing') {
            steps {
                echo 'Developing'
            }
        }
      
        stage('Testing') {
            steps {
                echo 'Testing'
            }
        }
    }
    }
